# Drift-Sense — the RUNTIME environment, as `pip freeze` reported it, and the
# environment every number in README.md and results/metrics.json was produced
# on.
#
#   Python 3.14.7, Linux x86_64
#   Direct dependencies (claude.md hard constraint): numpy, scipy,
#   opencv-python (NOT contrib), Pillow, matplotlib.  Everything else below is
#   a transitive dependency of matplotlib and is pinned only for reproducibility.
#
# scikit-learn is deliberately NOT here.  It is needed only to FIT the
# confidence model (train_confidence.py) and is listed in requirements-dev.txt;
# core/confidence.py does inference in numpy, so nothing on the runtime path
# imports it.  If your venv also has scikit-learn installed for development,
# a plain `pip freeze` will show it -- do not paste that back into this file.
#
# Regenerate with:  python -m pip freeze   (in a runtime-only venv)
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.1
numpy==2.5.2
opencv-python==5.0.0.93
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
scipy==1.18.0
six==1.17.0
